# 2026-06-07 worker-go-http 工作日记

## T-10: 社区市场后端 Handler + 路由

- **状态**: 完成
- **耗时**: 约 30 分钟

### 所做工作

1. 创建 `internal/hub/handler/community_handler.go`
   - 6 个 handler 函数：PublishWorker、ListCommunityWorkers、GetCommunityWorker、InstallWorker、ListCommunityWorkerReviews、AddCommunityWorkerReview
   - 本地实现 `deidentify()` 和 `deidentifyMap()` 辅助函数（与服务层相同的正则模式）
   - 完全复用 playbook_handler.go 的模式（请求结构体、JSON 绑定、错误处理、gin.H 响应格式）
   - user_id 从 JWT 中间件注入的上下文获取：`c.GetInt64("user_id")`

2. 修改 `internal/hub/router.go`
   - 在 admin/JWT 认证组新增 6 条路由
   - 路由与 handler 一一对应

### 技术要点
- 脱敏在 handler 层执行（req.Deidentify=true 时），而非 service 层
- InstallWorker 的 business_code 从 JSON body 解析，worker id 从路径参数解析
- ListCommunityWorkers 返回 `{data, total}` 结构支持分页
- 所有路由均在 JWT 认证保护下（admin 组）

### 踩坑
- 无
