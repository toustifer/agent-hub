# 2026-06-07 · worker-go-domain 日记

## T-9: 社区市场后端 Service 层

**状态**: 完成

**产出**:
- `internal/hub/service/community_service.go` — 新建，245 行
- 6 个公开方法 + 1 个私有辅助函数

**技术决策**:
- `ListCommunityWorkers` 采用双查询模式（SQL 取 ID + ent 取完整对象），与 `playbook_service.go` 的 `SearchPlaybooks` 一致
- `InstallWorker` 的事务使用 `s.Pool.Begin(ctx)` + `defer tx.Rollback(ctx)` + `tx.Commit(ctx)` 模式，确保 worker 创建、playbook 导入、计数器更新全部原子化
- 事务内所有操作用原始 SQL，因为 ent 有自己的连接池，无法参与 pgx 事务
- `deidentify` 未导出（小写首字母），仅内部使用

**踩坑/注意**:
- 项目使用 pgx v5，`s.Pool.Begin(ctx)` 返回 `pgx.Tx` 接口，不需要额外导入 `github.com/jackc/pgx/v5` -- 类型通过 `pgxpool` 传递
- 插入 `text[]` 列时需要显式 cast `$5::text[]`，否则可能因类型推断失败报错
- pgx v5 对 nil slice 会映射为 SQL NULL，需要显式传 `[]string{}` 而非 nil

**编译验证**: `GOARCH=amd64 go build ./...` -- 通过，无错误无警告
